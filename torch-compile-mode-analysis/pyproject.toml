[project]
name = "torch-compile-mode-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "pandas>=3.0.2",
    "pytest>=9.0.3",
    "torch==2.9.1",
    "triton>=3.5.1; sys_platform == 'linux'",
]
